---
import { getCollection } from 'astro:content';

const footerCollection = await getCollection('footer');

if (footerCollection.length === 0) {
  throw new Error('Footer not found in copy collection');
}

const footer = footerCollection[0];

const {
  headline,
  CTA: { label, url },
} = footer.data;

const currentYear = new Date().getFullYear();
---

<footer class='footer-section' id='contact'>
  <h2 class='heading'>{headline}</h2><a
    href={url}
    target='_blank'
    class='external-link'>{label}</a
  >
  <p>&copy; {currentYear} Marco Mazzai. All rights reserved.</p>
</footer>
