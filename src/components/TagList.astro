---
type Props = { tags: string[] };

const { tags } = Astro.props;
---

{
  tags.length > 0 && (
    <ul class='mm-tag-list'>
      {tags.map((tag) => (
        <li class='mm-tag'>{tag}</li>
      ))}
    </ul>
  )
}